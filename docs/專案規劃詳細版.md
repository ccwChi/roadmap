# Next Roadmap 專案規劃詳細版

## 📋 專案概述

**專案名稱**: Next Roadmap  
**技術棧**: Next.js 16 + React 19 + Zustand + ReactFlow  
**部署方式**: GitHub Pages (靜態導出)  
**核心功能**: 互動式學習路線圖應用，支援離線使用、雲端同步、音樂播放

---

## 🎯 專案目標

打造一個現代化的學習路線圖應用程式，讓使用者能夠：
1. 視覺化瀏覽學習路徑（使用流程圖展示）
2. 追蹤學習進度並記錄筆記
3. 透過 Google Drive 雲端同步資料
4. 支援離線使用（PWA + Service Worker）
5. 邊學習邊聽音樂（Google Drive 音樂播放器）

---

## 🏗️ 專案架構

### 目錄結構
```
next-roadmap/
├── app/                    # Next.js App Router 頁面
│   ├── layout.js          # 根佈局（主題、字體設定）
│   ├── page.js            # 首頁（Roadmap 選擇）
│   └── roadmap/           # 動態路由頁面
│
├── components/            # React 元件
│   ├── ui/                # shadcn/ui 基礎元件
│   ├── Header.js          # 頂部導航列
│   ├── Sidebar.js         # 側邊欄選單
│   ├── RoadmapFlow.js     # 流程圖主元件
│   ├── CustomNode.js      # 自訂節點元件
│   ├── NodeDetailModal.js # 節點詳情彈窗
│   ├── ProgressBar.js     # 進度條元件
│   ├── SettingsPanel.js   # 設定面板
│   └── MusicPlayer.js     # 音樂播放器
│
├── store/                 # Zustand 狀態管理
│   ├── useStore.js        # 主要應用狀態
│   └── useMusicStore.js   # 音樂播放器狀態
│
├── lib/                   # 工具函數庫
│   ├── googleDrive.js     # Google Drive API 整合
│   ├── googleDriveMusic.js # Google Drive 音樂檔案管理
│   ├── offlineSync.js     # 離線同步管理
│   └── utils.js           # 通用工具函數
│
├── data/                  # 靜態資料
│   ├── roadmaps/          # 各種 Roadmap 資料
│   └── roadmapData.js     # 預設 Roadmap
│
├── hooks/                 # 自訂 React Hooks
│   └── useOfflineSync.js  # 離線同步 Hook
│
├── worker/                # Service Worker
│   └── index.js           # 自訂 SW（Background Sync）
│
└── public/                # 靜態資源
```

---

## 🔧 核心技術實作

### 1. 狀態管理 (Zustand)

#### 主要狀態 Store (`useStore.js`)

**使用技術**:
- 利用 `zustand` 的 `create()` 建立狀態管理器
- 利用 `persist` 中間件進行資料持久化到 localStorage

**持久化資料包含**:
- `user`: 使用者資訊（Google 帳號）
- `isSignedIn`: 登入狀態
- `currentRoadmapId`: 當前選擇的 Roadmap ID
- `progress`: 各 Roadmap 的完成進度 `{ roadmapId: [nodeId1, nodeId2, ...] }`
- `notes`: 各節點的筆記 `{ roadmapId: { nodeId: "content" } }`
- `settings`: 應用設定（主題、自動同步）
- `lastSyncTime`: 最後同步時間
- `pendingSyncCount`: 待同步資料數量

**主要功能函數**:
- `setCurrentRoadmap(id)`: 切換當前 Roadmap
- `toggleNodeComplete(roadmapId, nodeId)`: 切換節點完成狀態，並觸發雲端同步
- `isNodeCompleted(roadmapId, nodeId)`: 檢查節點是否已完成
- `getProgress(roadmapId, totalNodes)`: 計算進度百分比
- `setNote(roadmapId, nodeId, content)`: 儲存筆記並同步
- `syncToCloud()`: 防抖動同步到 Google Drive（500ms 延遲）
- `loadFromCloud()`: 從 Google Drive 載入資料
- `handleOfflineSync(data)`: 處理離線佇列的同步請求

**離線支援機制**:
- 檢測網路狀態（`navigator.onLine`）
- 離線時將資料加入 Background Sync 佇列
- 利用 `offlineSync.js` 的 `queueSync()` 管理待同步資料
- 網路恢復時自動觸發同步

#### 音樂播放器 Store (`useMusicStore.js`)

**使用技術**:
- 同樣使用 `create(persist(...))` 進行狀態管理

**持久化資料包含**:
- `playlist`: 播放清單
- `volume`: 音量設定
- `playMode`: 播放模式（順序、循環、單曲、隨機）
- `hasMusicPermission`: Google Drive 音樂權限狀態

**播放模式**:
- `SEQUENCE`: 順序播放
- `LOOP`: 列表循環
- `SINGLE`: 單曲循環
- `SHUFFLE`: 隨機播放

**主要功能函數**:
- `playTrack(index)`: 播放指定曲目
- `nextTrack()`: 根據播放模式切換下一首
- `prevTrack()`: 切換上一首
- `addToPlaylist(track)`: 新增歌曲到播放清單
- `cyclePlayMode()`: 循環切換播放模式

#### UI 狀態 Store (`useUIStore`)

**非持久化狀態**（不需要儲存）:
- `sidebarOpen`: 側邊欄開關
- `selectedNode`: 當前選中的節點
- `searchQuery`: 搜尋關鍵字
- `settingsOpen`: 設定面板開關
- `isEditingNote`: 筆記編輯模式

---

### 2. Google Drive 整合

#### 認證與授權 (`googleDrive.js`)

**使用的 Google API**:
- Google API Client (`gapi.client`)
- Google Identity Services (GIS)

**OAuth 2.0 Scopes**:
- `drive.file`: 存取應用程式建立的檔案
- `drive.appdata`: 保留 appdata 權限（未來可切換隱藏資料夾）
- `userinfo.profile`: 讀取使用者個人資料
- `userinfo.email`: 讀取使用者 Email

**初始化流程**:
1. `loadGoogleScripts()`: 動態載入 GAPI 和 GIS 腳本
2. 初始化 `gapi.client` 並載入 Drive API discovery docs
3. 初始化 `tokenClient` 用於 OAuth 2.0 授權

**登入流程**:
1. `signIn()`: 觸發 OAuth 2.0 授權流程
2. 使用 `tokenClient.requestAccessToken()` 請求 token
3. 取得 token 後呼叫 `getUserInfo()` 獲取使用者資訊
4. 延遲 500ms 後呼叫 `loadFromCloud()` 載入雲端資料

**API 就緒檢查**:
- `isGoogleApiReady()`: 檢查 GAPI 是否完全初始化且有 token
- `waitForGoogleApiReady(timeout)`: 等待 API 就緒（輪詢檢查，最多等待指定時間）

#### 資料儲存機制

**資料夾與檔案結構**:
- 資料夾名稱: `AI-Roadmap-Data`（使用者可見）
- 檔案名稱: `roadmap-data.json`
- 儲存位置: Google Drive 根目錄下的專屬資料夾

**資料夾管理**:
- `findOrCreateDataFolder()`: 搜尋或建立資料夾
- 使用快取 `cachedFolderId` 避免重複查詢
- 利用 `gapi.client.drive.files.list()` 搜尋資料夾
- 利用 `gapi.client.drive.files.create()` 建立資料夾

**檔案操作**:
- `findOrCreateDataFile()`: 搜尋或建立 JSON 資料檔案
- `loadData()`: 使用 `files.get()` 讀取檔案內容（`alt: 'media'`）
- `saveData(data)`: 使用 `PATCH` 方法更新檔案內容
- 包含錯誤處理（401/403 權限錯誤、404 檔案不存在）

**資料合併策略**:
- `mergeData(localData, cloudData)`: 比較 `updatedAt` 時間戳
- 使用較新的資料覆蓋舊資料

**備註**: 程式碼中保留了切換到隱藏資料夾（`appDataFolder`）的註解說明，方便未來切換儲存方式。

#### 音樂檔案管理 (`googleDriveMusic.js`)

**音樂資料夾**:
- 預設資料夾名稱: `AI-Roadmap-Music`
- 支援自訂資料夾（使用者可選擇）

**檔案搜尋**:
- `listMusicFiles(folderId)`: 列出指定資料夾中的音樂檔案
- 支援的格式: `.mp3`, `.wav`, `.ogg`, `.m4a`, `.flac`
- 利用 `mimeType` 過濾音訊檔案

**音樂播放**:
- `getMusicFileUrl(fileId)`: 取得音樂檔案的播放 URL
- 使用 Google Drive 的 `webContentLink` 或直接串流
- 處理大檔案的分段載入

**資料夾選擇器**:
- 整合 Google Picker API（如果需要）
- 允許使用者選擇自訂音樂資料夾

---

### 3. 離線同步機制

#### Service Worker (`worker/index.js`)

**Background Sync 實作**:
- 使用 IndexedDB 儲存待同步資料
- 資料庫名稱: `roadmap-offline-db`
- Object Store: `pending-syncs`

**IndexedDB 操作函數**:
- `openDB()`: 開啟或建立資料庫
- `savePendingSync(data)`: 儲存待同步資料
- `getPendingSyncs()`: 取得所有待同步資料
- `deletePendingSync(id)`: 刪除已同步資料
- `clearPendingSyncs()`: 清空所有待同步資料

**訊息處理**:
- 監聽 `message` 事件處理來自主線程的請求
- `QUEUE_SYNC`: 將資料加入佇列並註冊 Background Sync
- `GET_PENDING_COUNT`: 回傳待同步數量
- `GET_PENDING_DATA`: 回傳所有待同步資料
- `FORCE_SYNC`: 強制執行同步
- `CLEAR_PENDING`: 清空佇列

**Background Sync 事件**:
- 監聽 `sync` 事件（tag: `roadmap-sync`）
- `performSync()`: 執行同步邏輯
- 通知所有 window clients 執行實際的 API 呼叫
- 同步完成後清空佇列

**網路狀態監聽**:
- 監聽 `online` 事件
- 網路恢復時自動註冊 Background Sync

#### 離線同步管理器 (`offlineSync.js`)

**Service Worker 通訊**:
- `sendMessageToSW(message, options)`: 發送訊息給 SW
- 使用 `MessageChannel` 進行雙向通訊
- 包含自動等待和重試機制（最多 2 次重試）
- 超時處理（預設 5 秒）

**降級方案**:
- 當 Service Worker 不可用時使用 localStorage
- `saveToLocalStorage(data)`: 儲存到本地存儲
- `getFromLocalStorage()`: 從本地存儲讀取
- `clearLocalStorage()`: 清空本地存儲

**公開 API**:
- `queueSync(data)`: 將資料加入離線同步佇列
- `getPendingCount()`: 取得待同步數量（SW + localStorage）
- `getPendingData()`: 取得所有待同步資料
- `forceSync()`: 強制同步
- `clearPending()`: 清空待同步佇列

**網路狀態監聽**:
- `listenForNetworkChanges(callback)`: 監聽 online/offline 事件
- `initNetworkStatus()`: 初始化網路狀態管理器

#### 離線同步 Hook (`useOfflineSync.js`)

**功能**:
- 監聽網路狀態變化（online/offline）
- 監聽 Service Worker 訊息（`SYNC_REQUIRED`）
- 監聽頁面可見性變化（`visibilitychange`）
- 監聽頁面關閉前事件（`beforeunload`）

**同步策略**:
- 網路恢復時延遲 1 秒後嘗試同步
- 每 30 秒定期檢查待同步資料
- 頁面變為可見時檢查並同步
- 頁面關閉前觸發立即同步

**防重複同步**:
- 使用 `useRef` 追蹤同步狀態（`isSyncingRef`）
- 避免同時觸發多次同步請求

---

### 4. 流程圖視覺化 (ReactFlow)

#### RoadmapFlow 元件

**使用的 ReactFlow 功能**:
- `useNodesState`: 管理節點狀態
- `useEdgesState`: 管理連線狀態
- `ReactFlow`: 主要流程圖元件
- `Background`: 背景網格
- `Controls`: 縮放控制
- `MiniMap`: 小地圖

**節點管理**:
- 從 `data/roadmaps/` 載入節點和連線資料
- 使用 `getRoadmapData(roadmapId)` 取得當前 Roadmap
- 節點類型: `custom`（自訂節點元件）

**互動功能**:
- `onNodeClick`: 點擊節點開啟詳情彈窗
- `nodesDraggable`: 可鎖定/解鎖節點拖曳
- 鎖定狀態預設為 `true`

**編輯工具**:
- 鎖定/解鎖按鈕（Lock/Unlock icon）
- 匯出座標按鈕（僅在解鎖時顯示）
- `exportPositions()`: 匯出節點座標為 JSON 並複製到剪貼簿

**主題適配**:
- 使用 `next-themes` 的 `useTheme` 取得當前主題
- 動態調整背景顏色、網格顏色、連線顏色

#### CustomNode 元件

**節點樣式**:
- 根據 `category` 顯示不同顏色（foundation, core, skill, advanced, application）
- 使用 `categoryColors` 定義邊框和背景顏色
- 完成狀態顯示打勾圖示

**節點資料結構**:
```javascript
{
  id: '1',
  type: 'custom',
  data: {
    label: '節點標題',
    category: 'core',
    description: '簡短描述',
    content: '詳細內容（Markdown）',
    resources: [{ title: '資源標題', url: 'URL' }]
  },
  position: { x: 400, y: 120 }
}
```

**互動**:
- 點擊節點觸發 `onNodeClick` 事件
- 顯示完成狀態（使用 `useStore` 的 `isNodeCompleted`）

#### NodeDetailModal 元件

**功能**:
- 使用 Radix UI 的 `Dialog` 元件
- 顯示節點詳細內容（description, content, resources）
- 完成狀態切換按鈕
- 筆記編輯功能（Textarea）

**筆記管理**:
- 使用 `useStore` 的 `getNote` 和 `setNote`
- 自動儲存到 Zustand store
- 觸發雲端同步

---

### 5. PWA 與快取策略

#### PWA 設定 (`next.config.mjs`)

**使用套件**:
- `@ducanh2912/next-pwa`: Next.js PWA 整合

**設定**:
- `dest: 'public'`: Service Worker 輸出目錄
- `disable: process.env.NODE_ENV === 'development'`: 開發環境停用
- `customWorkerSrc: 'worker'`: 自訂 Service Worker 來源
- `reloadOnOnline: true`: 網路恢復時重新載入

**Workbox 快取策略**:

1. **Google Fonts** (CacheFirst):
   - 快取名稱: `google-fonts`
   - 最多 4 個項目
   - 有效期: 1 年

2. **靜態資源** (StaleWhileRevalidate):
   - 快取名稱: `static-resources`
   - 檔案類型: `.js`, `.css`, `.woff2`
   - 最多 32 個項目
   - 有效期: 24 小時

3. **圖片** (CacheFirst):
   - 快取名稱: `images`
   - 檔案類型: `.jpg`, `.png`, `.svg`, `.webp` 等
   - 最多 64 個項目
   - 有效期: 30 天

**預快取資源**:
- `/`: 首頁
- `/manifest.json`: PWA manifest

#### Manifest 設定

**基本資訊**:
- 應用名稱、圖示、主題顏色
- 啟動 URL、顯示模式（standalone）

**圖示**:
- 多種尺寸的 PNG/SVG 圖示
- 支援不同裝置和解析度

---

### 6. 音樂播放器

#### MusicPlayer 元件

**權限管理**:
- `checkPermission()`: 檢查 Google Drive 權限
- `handleRequestPermission()`: 請求權限並載入音樂

**音樂載入**:
- `loadMusicFiles()`: 從 Google Drive 載入音樂列表
- 使用 `googleDriveMusic.js` 的 `listMusicFiles()`
- 支援搜尋功能（`handleSearch`）

**播放控制**:
- 使用 HTML5 `<audio>` 元素
- `loadAndPlay(track)`: 載入並播放音樂
- 進度條控制（`handleSeek`）
- 音量控制、靜音切換

**播放清單管理**:
- `handleAddToPlaylist(file)`: 新增單首歌曲
- `handleAddAll()`: 新增所有搜尋結果
- 支援拖曳排序（如果實作）

**UI 狀態**:
- 播放器開關（`isPlayerOpen`）
- 播放清單開關（`isPlaylistOpen`）
- 音樂瀏覽器開關（`isBrowserOpen`）

**播放模式**:
- 使用 `useMusicStore` 的 `playMode`
- 循環切換播放模式（`cyclePlayMode`）
- 根據模式決定下一首歌曲

---

### 7. UI 元件系統

#### shadcn/ui 元件

**使用的元件**:
- `Dialog`: 彈窗（節點詳情、設定面板）
- `DropdownMenu`: 下拉選單（使用者選單）
- `Progress`: 進度條
- `Switch`: 開關（設定項目）
- `Slider`: 滑桿（音量控制）
- `Button`: 按鈕

**樣式系統**:
- 使用 Tailwind CSS 4
- 使用 `class-variance-authority` 管理變體
- 使用 `clsx` 和 `tailwind-merge` 合併類名

#### 主題系統

**使用套件**:
- `next-themes`: Next.js 主題切換

**主題模式**:
- `dark`: 深色模式（預設）
- `light`: 淺色模式

**主題切換**:
- Header 元件中的主題切換按鈕
- 使用 `setTheme()` 切換主題
- 使用 `resolvedTheme` 取得當前主題

**CSS 變數**:
- 在 `globals.css` 中定義主題顏色變數
- 支援深色和淺色模式的不同配色

---

### 8. 路由與頁面

#### App Router 結構

**首頁** (`app/page.js`):
- Roadmap 選擇頁面
- 顯示可用的 Roadmap 列表
- 點擊進入對應的 Roadmap 頁面

**Roadmap 頁面** (`app/roadmap/[id]/page.js`):
- 動態路由，根據 `id` 載入對應的 Roadmap
- 顯示 RoadmapFlow 元件
- 包含 Header、Sidebar 等佈局元件

**404 頁面** (`app/not-found.js`):
- 自訂 404 錯誤頁面
- 提供返回首頁的連結

#### 靜態導出設定

**Next.js 設定**:
- `output: 'export'`: 靜態導出模式
- `trailingSlash: true`: 確保每個路由都有 index.html
- `images.unoptimized: true`: 停用圖片優化（靜態導出不支援）

**GitHub Pages 設定**:
- `basePath`: 根據環境變數設定子路徑（`/roadmap`）
- `assetPrefix`: 確保資源路徑正確

---

## 🔄 資料流程

### 使用者操作流程

1. **初次訪問**:
   - 載入應用程式
   - Service Worker 註冊
   - 從 localStorage 載入本地資料
   - 顯示預設 Roadmap

2. **登入 Google**:
   - 點擊登入按鈕
   - OAuth 2.0 授權流程
   - 取得 access token
   - 從 Google Drive 載入雲端資料
   - 合併本地與雲端資料

3. **學習進度追蹤**:
   - 點擊節點查看詳情
   - 標記節點為完成
   - 更新 Zustand store
   - 觸發雲端同步（500ms 防抖動）
   - 如果離線，加入 Background Sync 佇列

4. **筆記記錄**:
   - 在節點詳情中編輯筆記
   - 自動儲存到 Zustand store
   - 觸發雲端同步

5. **離線使用**:
   - 網路斷開時繼續使用
   - 資料變更加入離線佇列
   - 網路恢復時自動同步

6. **音樂播放**:
   - 請求 Google Drive 權限
   - 瀏覽音樂資料夾
   - 新增歌曲到播放清單
   - 播放音樂

### 資料同步流程

**線上同步**:
```
使用者操作 → Zustand store 更新 → syncToCloud() (防抖動 500ms)
→ 檢查網路狀態 → 檢查 Google API 就緒
→ saveData() → Google Drive API → 更新雲端檔案
→ 更新 lastSyncTime
```

**離線同步**:
```
使用者操作 → Zustand store 更新 → syncToCloud()
→ 檢測離線 → queueSync() → Service Worker (IndexedDB)
→ 網路恢復 → Background Sync 觸發
→ Service Worker 通知主線程 → handleOfflineSync()
→ saveData() → Google Drive API
→ clearPending() → 清空佇列
```

---

## 🎨 使用者介面設計

### Header 元件

**左側**:
- 選單按鈕（開啟 Sidebar）
- 當前 Roadmap 標題和圖示
- 進度條（compact 模式）

**右側**:
- 未登入: 登入按鈕
- 已登入: 使用者頭像 + 下拉選單
  - 使用者名稱
  - 主題切換
  - 設定
  - 登出
- 同步狀態圖示（旋轉、離線、錯誤、成功）

### Sidebar 元件

**功能**:
- Roadmap 列表
- 切換不同的學習路徑
- 顯示各 Roadmap 的進度

**互動**:
- 點擊切換 Roadmap
- 自動關閉（手機版）

### RoadmapFlow 元件

**主要區域**:
- 流程圖畫布（ReactFlow）
- 背景網格
- 縮放控制
- 小地圖

**右下角工具列**:
- 鎖定/解鎖按鈕
- 匯出座標按鈕（解鎖時顯示）

### SettingsPanel 元件

**設定項目**:
- 主題切換（深色/淺色）
- 自動同步開關
- 清除所有資料
- 關於資訊

### MusicPlayer 元件

**迷你播放器**:
- 音樂圖示按鈕（開啟播放器）
- 當前播放歌曲資訊
- 播放/暫停按鈕

**完整播放器**:
- 播放控制（上一首、播放/暫停、下一首）
- 進度條
- 音量控制
- 播放模式切換
- 播放清單
- 音樂瀏覽器

---

## 📦 部署流程

### GitHub Actions 工作流程

**觸發條件**:
- 推送到 `main` 分支
- 手動觸發（workflow_dispatch）

**建置步驟**:
1. Checkout 程式碼
2. 設定 Node.js 環境
3. 安裝依賴（`npm ci`）
4. 設定環境變數（Google API keys）
5. 建置專案（`GITHUB_PAGES=true npm run build`）
6. 部署到 GitHub Pages（`out` 目錄）

**環境變數**:
- `GOOGLE_CLIENT_ID`: 從 GitHub Secrets 讀取
- `GOOGLE_API_KEY`: 從 GitHub Secrets 讀取

### 本地測試

**開發模式**:
```bash
npm run dev
```

**Production Build 測試**:
```bash
GITHUB_PAGES=true npm run build
npx serve out
```

---

## 🔐 安全性考量

### Google API 金鑰

**Client ID**:
- 儲存在環境變數
- 建置時注入到前端程式碼
- 限制授權來源（GitHub Pages domain）

**API Key**:
- 僅用於公開 API（Drive API discovery）
- 限制使用範圍和配額

### 資料隱私

**使用者資料**:
- 儲存在使用者的 Google Drive
- 應用程式無法存取其他使用者的資料
- 使用 OAuth 2.0 授權，使用者可隨時撤銷

**本地資料**:
- 儲存在 localStorage 和 IndexedDB
- 僅在使用者的瀏覽器中
- 清除瀏覽器資料即可刪除

---

## 🚀 未來擴展方向

### 功能擴展

1. **多人協作**:
   - 分享 Roadmap 給其他使用者
   - 即時協作編輯

2. **自訂 Roadmap**:
   - 使用者建立自己的學習路徑
   - 視覺化編輯器

3. **學習統計**:
   - 學習時間追蹤
   - 進度圖表
   - 成就系統

4. **社群功能**:
   - 分享學習筆記
   - 討論區
   - 資源推薦

5. **AI 輔助**:
   - 根據進度推薦下一步
   - 自動生成學習計畫
   - 智能筆記整理

### 技術優化

1. **效能優化**:
   - 虛擬化大型 Roadmap
   - 圖片懶加載
   - 程式碼分割

2. **離線體驗**:
   - 更完善的離線快取
   - 離線編輯 Roadmap
   - 衝突解決機制

3. **跨平台**:
   - Electron 桌面應用
   - React Native 行動應用
   - 瀏覽器擴充功能

---

## 📚 技術文件參考

### 主要依賴套件

- **Next.js 16**: https://nextjs.org/docs
- **React 19**: https://react.dev
- **Zustand**: https://zustand-demo.pmnd.rs
- **ReactFlow**: https://reactflow.dev
- **Radix UI**: https://www.radix-ui.com
- **Tailwind CSS 4**: https://tailwindcss.com
- **next-themes**: https://github.com/pacocoursey/next-themes
- **@ducanh2912/next-pwa**: https://ducanh-next-pwa.vercel.app

### Google APIs

- **Google Drive API**: https://developers.google.com/drive/api/guides/about-sdk
- **Google Identity Services**: https://developers.google.com/identity/gsi/web

---

## 📝 開發規範

### 程式碼風格

**格式化工具**:
- Biome (替代 ESLint + Prettier)
- 設定檔: `biome.json`

**命令**:
```bash
npm run lint      # 檢查程式碼
npm run format    # 格式化程式碼
```

### Git 工作流程

**分支策略**:
- `main`: 生產環境
- `develop`: 開發環境
- `feature/*`: 功能分支

**Commit 訊息**:
- 使用語意化提交訊息
- 範例: `feat: 新增音樂播放器`、`fix: 修復離線同步問題`

---

## 🎯 總結

這個專案是一個功能完整的現代化 Web 應用程式，展示了以下技術亮點：

1. **狀態管理**: 使用 Zustand 進行輕量級狀態管理，搭配 persist 中間件實現資料持久化
2. **雲端整合**: 完整的 Google Drive API 整合，包含 OAuth 2.0 認證、檔案管理、音樂播放
3. **離線支援**: 利用 Service Worker、Background Sync、IndexedDB 實現完善的離線體驗
4. **視覺化**: 使用 ReactFlow 打造互動式學習路徑圖
5. **PWA**: 完整的 PWA 支援，包含 manifest、Service Worker、快取策略
6. **現代化 UI**: 使用 Radix UI + Tailwind CSS 打造美觀且可訪問的介面
7. **靜態部署**: 透過 Next.js 靜態導出部署到 GitHub Pages

整體架構清晰、模組化良好、擴展性強，是一個優秀的學習和參考專案。





learning-mind-map/
├── ai-agent/           # AI Agent 學習地圖（獨立項目）
│   ├── roadmap/        # 路線圖視圖 (現階段不做了)
│   └── mind-map/       # 愛因斯坦腦圖視圖
├── cv-engineer/        # CV 工程師學習地圖（獨立項目）
│   ├── roadmap/
│   └── mind-map/
└── python-fastapi/     # Python FastAPI 學習地圖（獨立項目）
    ├── roadmap/
    └── mind-map/



learning-mind-map/
├── mind-map/           # AI Agent 學習地圖（獨立項目）
│   ├── ai-agent/ 
│   ├── cv-engineer/ 
│   └── python-fastapi/   

app/
├── learning-mind-map/   # AI Agent 學習地圖（獨立項目）
│   ├── ai-agent/        # 每個項目都是目前的cards內容
│   ├── cv-engineer/ 
│   └── python-fastapi/   
│
├── layout.jsx
├── page.jsx